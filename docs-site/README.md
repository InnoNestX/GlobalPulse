# docs-site

VitePress documentation for GlobalPulse.

```bash
cd docs-site
npm install
npm run docs:dev
```

Build:

```bash
npm run docs:build
```

Output is written to `_site/` and published to GitHub Pages by `.github/workflows/docs.yml`.
